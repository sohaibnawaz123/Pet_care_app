class RoleselectionViewInitialParams {
  const RoleselectionViewInitialParams();

  factory RoleselectionViewInitialParams.fromUri(Uri uri) {
    return RoleselectionViewInitialParams(
      // Parse the URI parameters as needed
    );
  }
}